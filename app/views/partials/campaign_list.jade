extends ../layouts/default

block content

h1 活动列表
  ul
    each campaign in campaigns
      li
        h2= campaign.campaign.content
        p= '创建时间' + campaign.create_time
        p= '开始时间' + campaign.start_time
        p= '结束时间' + campaign.end_time