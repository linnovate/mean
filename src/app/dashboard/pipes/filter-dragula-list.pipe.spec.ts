import { FilterDragulaListPipe } from './filter-dragula-list.pipe';

describe('FilterDragulaListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDragulaListPipe();
    expect(pipe).toBeTruthy();
  });
});
